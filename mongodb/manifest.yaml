apiVersion: apps/v1
kind: Deployment
metadata:  
  name: mongodb
  namespace: roboshop
  labels:
    project: roboshop
    env: dev
spec:
 replicas: 1
 selector:
   matchLabels:
     component: mongodb
     project: roboshop
     env: dev
 template:
   metadata:
     labels:
       component: mongodb
       project: roboshop
       env: dev
   spec:
     containers:
      - name: mongodb
        image: srinunaik208/mongodb:v1
---
apiVersion: v1
kind: Service
metadata:
  name: mongodb
  namespace: roboshop
  labels: 
    project: roboshop
    env: dev
spec:
  selector:
    component: mongodb
    project: roboshop
    env: dev
  ports:
   - protocol: TCP
     port: 27017
     targetPort: 27017

