apiVersion: v1 
kind: Service
metadata:
  name: rabbitmq-headless
  namespace: roboshop
  labels:
    project: roboshop
    component: rabbitmq
    tier: database
spec:
  clusterIP: None
  selector:
    project: roboshop
    component: rabbitmq
    tier: database
  ports:
  - protocol: TCP 
    port: 5672
    targetPort: 5672
---
apiVersion: v1
kind: Service
metadata:
  name: rabbitmq
  namespace: roboshop
  labels:
    project: roboshop
    component: rabbitmq
    tier: database
spec:
  selector:
    project: roboshop
    component: rabbitmq
    tier: database
  ports:
  - protocol: TCP 
    port: 5672
    targetPort: 5672
---
apiVersion: apps/v1 
kind: StatefulSet 
metadata:
  name: rabbitmq
  namespace: roboshop 
  labels:
    project: roboshop
    component: rabbitmq
    tier: database
spec:
 serviceName: rabbitmq-headless
 replicas: 1
 selector:
   matchLabels:
      project: roboshop
      component: rabbitmq
      tier: database
 template:
   metadata:
     labels:
       project: roboshop
       component: rabbitmq
       tier: database
   spec:
     containers:
     - name: rabbitmq
       image: srinunaik208/rabbitmq:v5
       volumeMounts:
       - name: rabbitmq-storage
         mountPath: /var/lib/rabbitmq

 volumeClaimTemplates:
 - metadata:
     name: rabbitmq-storage
   spec:
     accessModes:
     - ReadWriteOnce
     storageClassName: roboshop
     resources:
        requests:
          storage: 4Gi


     