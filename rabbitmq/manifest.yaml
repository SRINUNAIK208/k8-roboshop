apiVersion: apps/v1
kind: Deployment
metadata: 
  name: rabbitmq
  namespace: roboshop
  labels:
    component: rabbitmq
    project: roboshop
    env: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      component: rabbitmq
      project: roboshop
      env: dev
  template:
    metadata:
      labels:
        component: rabbitmq
        project: roboshop
        env: dev
    spec:
      containers:
        - name: rabbitmq
          image: rabbitmq:3
---
apiVersion: v1
kind: Service
metadata:
  name: rabbitmq
  namespace: roboshop
  labels:
    component: rabbitmq
    project: roboshop
    env: dev
spec:
  selector:
    component: rabbitmq
    project: roboshop
    env: dev
  ports:
   - protocol: TCP
     port: 5672
     targetPort: 5672
